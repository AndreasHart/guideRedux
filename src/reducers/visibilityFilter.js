const visibilityFilter = (state = 'SHOW_ALL', action) => {
  switch (action.type) {
    case 'UPDATE_FILTER':
    debugger;
      return action
    default:
      return state
  }
}

export default visibilityFilter
